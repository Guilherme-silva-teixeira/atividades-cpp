#include <iostream>
#include <locale>

using namespace std;

struct Produtos
{
	int changeArray(int tot)
	{
		if (tot == 0)
		{
			tot == 1;
		}
		return tot;
	}

	int tot = changeArray(1);

	string nome[100];
	string tipo[100];
	int id[100];
	int estq[100];
};

int main(int null)
{

	return null;
}
